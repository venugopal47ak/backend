{
  "type": "module",
  "name": "nammaserve-platform",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "namma_backend/server"
  ],
  "scripts": {
    "start": "node server.js",
    "dev": "npm run dev --workspace namma_backend/server",
    "dev:server": "npm run dev --workspace namma_backend/server",
    "build": "npm run build --workspace namma_backend/server",
    "seed": "npm run seed --workspace namma_backend/server",
    "init:providers": "npm run init:providers --workspace namma_backend/server"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.37.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "razorpay": "^2.9.2"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
